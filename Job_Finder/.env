#VITE_API_URL=http://localhost:5000
# ============================================================
# File: Job_Finder/.env
# Place this in the ROOT of your React project (same level as package.json)
# ============================================================

# Spring Boot backend URL — must match port in application.properties
VITE_API_URL=http://localhost:8092/LocalJobApp/request